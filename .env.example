# Invite link for the hidden /welcome page. Read at build time, so changing it
# needs a redeploy. Leave unset and the page renders the label as plain text
# rather than shipping a dead link.
WHATSAPP_INVITE_URL=
